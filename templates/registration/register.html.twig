{% extends 'base.html.twig' %}

{% block title %}Log in! Admin Reservation{% endblock %}

{% block body %}
<body class="login-img3-body">

    <div class="container">


      <form class="login-form" action="{{ path('app_login') }}" method="post" > 

      {{ form_start(registrationForm) }} 
             {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
            {% endfor %}

   
        <div class="login-wrap">
            <p class="login-img"><i class="icon_lock_alt"></i></p>
            <div class="input-group">
            
              {{ form_row(registrationForm.email) }}
            
            </div>
            <div class="input-group">
                
                {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
            </div>
            <label class="checkbox">
                {{ form_row(registrationForm.agreeTerms) }} 
                
            </label>
            <button class="btn btn-primary btn-lg btn-block" type="submit">Register
        </div>
            {{ form_end(registrationForm) }}
      </form>
    <div class="text-right">
            <div class="credits">
                <!-- 
                    All the links in the footer should remain intact. 
                    You can delete the links only if you purchased the pro version.
                    Licensing information: https://bootstrapmade.com/license/
                    Purchase the pro version form: https://bootstrapmade.com/buy/?theme=NiceAdmin
                -->
                <a href="https://www.linkedin.com/in/ilyes-khaled-4a959310a/">Admin Contact</a> 
            </div>
        </div>
    </div>





  </body>
{% endblock %}
