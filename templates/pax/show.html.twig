{% extends 'base.html.twig' %}

{% block title %}Pax{% endblock %}

{% block body %}
    <h1>Pax</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pax.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pax.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ pax.prenom }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ pax.cin }}</td>
            </tr>
            <tr>
                <th>Nombre_de_enfant</th>
                <td>{{ pax.nombreDeEnfant }}</td>
            </tr>
            <tr>
                <th>Sex</th>
                <td>{{ pax.sex }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('reservationlist') }}">back to list</a>

    <a href="{{ path('reservationedit', {'id': pax.id}) }}">edit</a>

    {{ include('pax/_delete_form.html.twig') }}
{% endblock %}
