{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ chambre.numero }}</td>
            </tr>
            <tr>
                <th>Prix_de_base</th>
                <td>{{ chambre.prixDeBase }}</td>
            </tr>
            <tr>
                <th>Prix_exeptionnel</th>
                <td>{{ chambre.prixExeptionnel }}</td>
            </tr>
            <tr>
                <th>Nombre_de_personne</th>
                <td>{{ chambre.nombreDePersonne }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ chambre.type }}</td>
            </tr>
            <tr>
                <th>Etage</th>
                <td>{{ chambre.etage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chambre_index') }}">back to list</a>

    <a href="{{ path('chambre_edit', {'id': chambre.id}) }}">edit</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
